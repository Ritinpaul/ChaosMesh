# ChaosMesh Arena — runtime requirements
# Keeping this minimal and pinned for reliable Docker builds

# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9

# --- HTTP ---
httpx>=0.27.0
requests>=2.31.0

# --- LLM ---
openai>=1.35.0

# --- Auth ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
alembic>=1.13.0

# --- Vector store ---
chromadb>=0.5.0

# --- Agents ---
langchain-core>=0.2.0
langgraph>=0.1.9

# --- Simulation ---
networkx>=3.3
gymnasium>=0.29.1

# --- Rate limiting ---
slowapi>=0.1.9

# --- Cache (optional — graceful if redis not running) ---
redis[hiredis]>=5.0.0

# --- Observability ---
prometheus-fastapi-instrumentator>=7.0.0

# --- Templating ---
jinja2>=3.1.4

# --- Utilities ---
python-dotenv>=1.0.0
structlog>=24.2.0
click>=8.1.0
rich>=13.7.0
